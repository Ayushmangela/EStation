import 'package:flutter/material.dart';

class ManageStationsView extends StatelessWidget {
  const ManageStationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Manage Stations'),
      ),
      body: const Center(
        child: Text('Station management functionality will be implemented here.'),
      ),
    );
  }
}
