import 'package:flutter/material.dart';

class ChargingView extends StatelessWidget {
  const ChargingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Charging Page', style: TextStyle(fontSize: 24)),
    );
  }
}
